name: osv scan v2

on:
  schedule:
    - cron: "0 3 * * *" # daily at 1:00 AM UTC
  workflow_dispatch:

permissions:
    contents: write
    security-events: write

jobs:
  refresh:
    uses: sumathi-thirumani/bc-scaffold/.github/workflows/osv-scanner.yml@feature/security-vulnerability-capture
    with:
      scan-args: |-
        --fail-on-vuln: false
      
